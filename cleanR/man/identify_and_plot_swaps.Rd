% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_swaps.R
\name{identify_and_plot_swaps}
\alias{identify_and_plot_swaps}
\title{Identify and Visualize Potential Swaps in Longitudinal Weight Data}
\usage{
identify_and_plot_swaps(
  data,
  weight_columns,
  key_column,
  plot_filename = "inst/extdata/swaps_plot.png",
  weight_diff_threshold = 1
)
}
\arguments{
\item{data}{A data frame containing the dataset with at least 2 weight columns and a key column such as "ID".}

\item{weight_columns}{A character vector specifying the names of the weight columns (e.g., c("X10_6_weight", "X10_9_weight")).}

\item{key_column}{A character string specifying the column name used to identify rows (e.g., "pot_id").}

\item{plot_filename}{A character string specifying the file name to save the plot (e.g., "swaps_plot.png").}

\item{weight_diff_threshold}{A numeric value specifying the minimum weight difference (in grams) that defines a potential swap.}
}
\value{
A list containing:
\item{swaps}{A data frame of rows flagged as potential swaps, where weight difference exceeds the specified threshold.}
\item{plot}{A ggplot object visualizing the weights and flagged potential swaps.}
\item{saved_plot}{The path to the saved image file of the plot.}
}
\description{
This function identifies potential swaps in longitudinal weight data based on a
threshold difference in weight between consecutive time points. It highlights
the flagged rows in a plot and returns a dataset with the potential swaps.
}
